import 'package:flutter/material.dart';

class Colorconstants {
 static Gradient gradient =
    const LinearGradient(colors: [Color(0xff814ceb), Color(0xff2d155d)]);

static Color primaryColor = const Color(0xff2d155d);
static Color secondaryColor = const Color(0xff814ceb);
static Color bgColor = const Color.fromARGB(255, 40, 16, 61);

}